// Package report renders the impact report as JSON, as the text ledger
// block for incident channels, and as markdown for postmortems. Every leg
// carries its evidence tag; realized and estimated value are never summed
// into one number.
package report

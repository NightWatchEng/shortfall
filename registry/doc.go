// Package registry loads and validates the flow registry: the versioned,
// Finance-co-signed YAML that defines flows, stages, money kinds, SLAs,
// estimators, baseline configuration, recovery curves, and reconcile sources.
package registry

# Architecture decision records

One ADR per irreversible design decision. The first six cover: money
representation, event transport, context propagation, the metric label set,
in-flight age buckets, and the baseline model.
